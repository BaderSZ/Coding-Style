/*
 * If...else
 *
 * This code might seem ugly, but remember that it
 * is an example. Actual code will have multiple 
 * lines per case and will look much neater.
 */

/* This is an example class, with a comment.
 * Every function or class should have a comment before it.
 */
class Example
{
    void bar()
    {
        do
        {
        }
        while (true);
        try
        {
        }
        catch (...)
        {
        }
    }
    void foo2()
    {
        if (true)
        {
            return;
        }
        if (true)
        {
            return;
        }
        else if (false)
        {
            return;
        }
        else
        {
            return;
        }
    }
    void foo(int state)
    {
        if (true)
            return;
        if (true)
            return;
        else if (false)
            return;
        else
            return;
    }
};
